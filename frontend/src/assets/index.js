/**
 * assets/index.js — barrel export for static assets.
 *
 * Import images, icons, and other static files here so consumers
 * can reference them via the @assets alias.
 *
 * Example:
 *   import { logo } from '@assets';
 *
 * export { default as logo } from './images/logo.svg';
 */
