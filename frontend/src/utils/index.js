/**
 * utils/index.js — barrel export for utility functions.
 *
 * Example:
 *   import { formatDate, slugify } from '@utils';
 *
 * export { formatDate }  from './formatDate';
 * export { slugify }     from './slugify';
 * export { cn }          from './cn';           // class-name helper
 */
